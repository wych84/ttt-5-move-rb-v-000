#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

puts "Welcome to Tic Tac Toe!"
board = ["0", "1", "2", "3", "4", "5", "6", "7", "8"]
puts ""Where would you like to go?"
input = gets.strip
input_to_index(input)
move(boardwithx, index, char=X)
display_board